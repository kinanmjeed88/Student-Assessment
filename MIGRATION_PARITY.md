# تقرير تكافؤ الترحيل

## نطاق العمل

تمت مقارنة النسخة القديمة `v1.0-react-native-legacy` مع تطبيق Flutter الحالي، ثم أُعيد تنفيذ الفجوات الوظيفية داخل طبقات البيانات والعرض بدلاً من إضافة شاشات شكلية. الهدف هو أن تكون كل ميزة قابلة للوصول من مسار واضح، وأن تمر عمليات الحفظ والحذف والاستعادة عبر `LocalRepository` وRiverpod.

## مصفوفة التكافؤ

| المجال | النسخة القديمة | Flutter الجديد | الحالة |
|---|---|---|---|
| الهوية البصرية | أيقونة زرقاء بكتاب وعلامة خضراء، وSplash مطابق | أصول `assets/images`، موارد Android launcher، وSplash مربوط بـ `LaunchTheme` | مكتمل |
| Dashboard | مؤشرات الطلاب والحضور وإجراءات سريعة | مؤشرات الطلاب والفصول والحضور والسلوك، وروابط الدرجات والتقارير والاستيراد | مكتمل |
| الطلاب | بحث وفلاتر وإنشاء وتعديل وحذف | بحث وفلاتر وحقول الاسم والرقم والولي والحالة والصف والشعبة، مع حذف تتابعي | مكتمل |
| ملف الطالب | درجات وحضور وسلوك وملاحظات وتصدير | تبويبات وملخصات ونماذج إضافة وحذف وتصدير PDF وExcel | مكتمل |
| الحضور | تسجيل الحاضر والغائب والمتأخر والمعتذر | سجل حضور محلي مع الحالات والتاريخ والملاحظات | مكتمل |
| السلوك | إيجابي ومتابعة وسلبي، وأنواع مخالفات ونقاط وحدود تحذير | `BehaviorRecord`، `PenaltyRules`، ملخصات وتنبيهات ونماذج وحذف | مكتمل |
| الدرجات | حقول تقييم ودرجات وملاحظات وفصول | `GradeField` و`Grade` مع إنشاء ذري وإدخال الحد الأقصى الفعلي | مكتمل |
| الاستيراد | Excel وWord وCSV/TXT ومعاينة وتكرار وسجل وتراجع | XLSX/XLS وDOCX وCSV/TXT، تنظيف وتكرار ومعاينة وسجل وتراجع | مكتمل للصيغ المدعومة |
| التقارير | PDF وExcel للطلاب والحضور والدرجات والسلوك والملاحظات | `ReportService` بصيغ XLSX وPDF وJSON، مع تصدير ملف الطالب | مكتمل |
| النسخ الاحتياطي | JSON واستعادة محلية | نسخة احتياطية شاملة لكل كيانات Isar، مع استعادة متوافقة مع القيم القديمة | مكتمل |
| التنقل | تبويبات رئيسية وخدمات إضافية | NavigationBar بخمس وجهات، وQuick Actions وإعدادات للوحدات الإضافية | مكتمل |

## قرارات هندسية

تستخدم الواجهة `ColorScheme` وMaterial 3، وتبقى كل عمليات التخزين خلف `LocalRepository`. تم الحفاظ على سلوك RTL عبر `Directionality` وخصائص `EdgeInsetsDirectional`، وتجنب `GestureDetector` والقيود اليدوية التي قد تسبب اختلافاً بين اتجاهي العرض. كما تم اعتماد `LayoutBuilder` في الإحصاءات لتفادي سحق البطاقات على الشاشات الضيقة، مع السماح للبطاقة بالتمدد عند تكبير الخط.

لا يُستخدم `AutoSizeText` لتجاوز تكبير الخط؛ لأن تصغير النص آلياً ليس بديلاً عن قابلية الوصول. كذلك تم تضمين خط عربي داخل أصول التطبيق لتفادي فقدان الحروف العربية في تقارير PDF.

## حدود التحقق الحالية

نجح الفحص الساكن الخاص بالمشروع، ونجح `git diff --check`، ولم تُترك أزرار placeholder أو عبارات TODO ضمن طبقة `lib`. لم تُشغّل أوامر `flutter analyze` و`flutter test` و`build_runner` داخل بيئة المراجعة لأن Flutter وDart SDK غير متاحين فيها. لذلك يجب تشغيلها داخل بيئة Flutter قبل إصدار نسخة متجر، مع إعادة توليد ملفات Isar بعد تعديل `StudentImportFormat`.

## References

[1]: https://github.com/kinanmjeed88/Student-Assessment/tree/main/lib "Flutter application source"

[2]: https://github.com/kinanmjeed88/Student-Assessment/tree/v1.0-react-native-legacy "Legacy application source tag"

[3]: https://pub.dev/packages/excel_plus "excel_plus package documentation"

[4]: https://pub.dev/packages/archive "archive package documentation"
