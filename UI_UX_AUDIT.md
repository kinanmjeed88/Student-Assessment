# تقرير تدقيق UI/UX أولي

## نطاق التدقيق

تمت مراجعة البنية الحالية لواجهات Dashboard، والطلاب، وملف الطالب، والغلاف الرئيسي، والثيم، مع الاستناد إلى الكود الموجود في الفرع `main`. الصور المرجعية غير مرفقة في جلسة التنفيذ الحالية؛ لذلك يركز هذا الإصدار على العيوب القابلة للتحقق من الكود وعلى إعادة بناء النظام البصري بما يعالج التشوهات المرئية المتوقعة.

## النتائج الرئيسية

| المجال | المشكلة الحالية | الأثر | المعالجة المعتمدة |
|---|---|---|---|
| المساحات والنسب | الاعتماد على حشو متكرر وقيم رأسية ثابتة، مع بطاقة إحصائية ذات `minHeight: 178` | فراغات غير متوازنة واحتمال قص المحتوى عند تكبير الخط | نظام مسافات موحد، وقيود مرنة، وارتفاع ناتج عن المحتوى داخل `LayoutBuilder` |
| Dashboard | عرض الإحصاءات في صف أو تركيب خاص لشاشات ضيقة، والإجراءات كسلسلة بطاقات متشابهة دون تجميع بصري | كثافة عالية ومساحات ميتة وتغير مفاجئ في النسب بين الأجهزة | شبكة متجاوبة للإحصاءات، وبطاقات إجراءات ذات مستويات أولوية واضحة |
| القوائم | بطاقة الطالب الحالية تعتمد على `ListTile` كثيف مع نصوص وشارات وقائمة إجراءات في مساحة محدودة | صعوبة المسح البصري، وضغط العنوان والوصف، وتراجع قابلية التوسع | بطاقة قائمة ذات رأس واضح، ومعلومات ثانوية قابلة للالتفاف، وإجراءات منفصلة لا تكسر التخطيط |
| ملف الطالب | استخدام `SegmentedButton` بأربع مقاطع طويلة داخل عرض الهاتف، مع عرض كل الأقسام داخل صفحة واحدة | تبويبات مضغوطة واحتمال Overflow وضعف الفصل بين البيانات | `DefaultTabController` و`TabBar` قياسي قابل للتمرير، مع محتوى تبويبي واضح داخل `TabBarView` |
| النوافذ والنماذج | نماذج إضافة وتعديل كبيرة داخل `AlertDialog` بعرض ثابت `520` أو `480` | قابلية قراءة محدودة على الهواتف والاتجاهات المختلفة | `Dialog` بحدود مرنة، و`ConstrainedBox` بحد أقصى، وحشو متكيف مع العرض |
| الاستجابة | وجود تخطيطات تتفرع عند عتبة واحدة، وقيود ثابتة في بعض البطاقات والنماذج | اختلافات حادة بين الأجهزة الصغيرة والكبيرة | `SafeArea` و`LayoutBuilder` و`ConstrainedBox` و`Wrap` مع نقاط توقف واضحة |
| الهوية البصرية | استخدام أيقونة `auto_awesome` في الترحيب و`psychology` في السلوك | إيحاءات غير مؤسسية وغير مناسبة لنظام سجل مدرسي | استبدالها بأيقونات إدارية وأكاديمية مثل `school`, `analytics`, `manage_accounts`, `fact_check` |
| الثيم | الثيم يعمل بـ Material 3 لكنه لا يفرض نظاماً متكاملاً للمكونات والحالات | تفاوت في الحواف، الارتفاعات، النصوص، وألوان الحالات | إعادة تأسيس `ThemeData` من `ColorScheme` مع ثيمات موحدة للبطاقات، الحقول، الأزرار، التنقل، والتبويبات |
| قابلية التوسع | العناصر تعتمد على صفوف ضيقة ومعلومات كثيرة داخل `ListTile` واحد | إضافة زر أو حالة جديدة قد تكسر العرض | مكونات مشتركة ذات مناطق مستقلة للعناوين، الوصف، الشارات، والإجراءات |

## تقييم الأولويات

الأولوية الأولى هي ملف الطالب وDashboard لأنهما يمثلان مركز القراءة اليومية واتخاذ القرار. الأولوية الثانية هي قائمة الطلاب والفصول، ثم الإعدادات لأنها تحتاج إلى تنظيم معلوماتي واضح أكثر من حاجتها إلى عناصر جديدة. لن يتم تغيير طبقة Riverpod أو Isar إلا عند الحاجة إلى تكييف واجهة العرض؛ وستبقى العمليات الحالية والتصدير والحذف والاستيراد مرتبطة بنفس المزودات والخدمات.

## مبادئ إعادة التصميم

سيُبنى النظام على لغة Material 3 هادئة تعتمد على `ColorScheme` فقط، مع اتجاه RTL على مستوى التطبيق، ومنع الألوان الثابتة داخل الواجهات. ستستخدم الشاشة `SafeArea`، وستُحسب المساحات والتخطيطات ضمن القيود الفعلية بدلاً من فرض أبعاد صلبة. ستُستخدم الأيقونات الأكاديمية والإدارية فقط، ولن تظهر أي أيقونة تحمل دلالة الذكاء الاصطناعي أو النجوم المتلألئة أو العقول.

سيتم فصل البنية إلى مكونات قابلة لإعادة الاستخدام: رأس شاشة، بطاقة إحصاء، بطاقة طالب، شارة حالة، قسم إعدادات، وحالة فارغة. هذا يسمح بإضافة أزرار أو حالات مستقبلية دون إعادة بناء كل شاشة أو إدخال عناصر جديدة داخل صفوف ضيقة.

## خطة التنفيذ

أولاً، إعادة تأسيس الثيم ونظام المقاسات. ثانياً، إعادة بناء Dashboard وقوائم الطلاب والفصول. ثالثاً، تحويل ملف الطالب إلى بنية تبويبية قياسية مع `DefaultTabController` و`TabBar` قابل للتمرير. رابعاً، تنظيم الإعدادات في أقسام مؤسسية واضحة. أخيراً، تشغيل الفحص الساكن والاختبارات وBuild Check ثم رفع كل التغييرات إلى `main`.
