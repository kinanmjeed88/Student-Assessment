# خط أساس إعادة الهيكلة

## المرجع

تم تثبيت الحالة الحالية للمشروع على آخر التزام موجود في الفرع `main` قبل بدء إعادة التنظيم.

## الفحوصات المتاحة في البيئة

الفحص المحلي المتاح هو `scripts/static_check.sh`، وقد نجح. يتضمن الفحص التحقق من موارد أيقونة Android وقواعد Flutter الساكنة. أدوات Flutter وDart غير متاحة في بيئة العمل المحلية، لذلك سيبقى `flutter analyze` و`flutter test` و`dart format` و`flutter build apk` بوابة تحقق رسمية عبر GitHub Actions.

## النتائج المؤكدة

- يوجد نمط `AsyncValue.when` يعرض `SizedBox.shrink()` في حالات التحميل والخطأ داخل شاشات الطلاب والسلوك والاستيراد والدرجات والتقارير.
- توجد قيم `EdgeInsets.fromLTRB` في شاشات الطلاب والسلوك والدرجات، وتحتاج إلى تحويل منهجي إلى `EdgeInsetsDirectional`.
- يوجد استدعاء مباشر لـ `ImportExportService` داخل طبقة العرض في شاشة استيراد الطلاب.
- توجد استدعاءات مباشرة لـ `ReportService` داخل شاشة التقارير.
- توجد مساحات واتجاهات ثابتة تحتاج إلى توحيد عبر Design Tokens.
- الفحص الساكن الحالي ناجح قبل التعديلات.
- بيانات Isar وRiverpod والتصدير الحالية يجب الحفاظ عليها أثناء إعادة التنظيم.

## سياسة التنفيذ

سيتم تنفيذ التغييرات على مراحل صغيرة قابلة للتحقق. لا يتم حذف أي وظيفة أو نموذج بيانات لمجرد تحسين التخطيط. أي تغيير في API داخلي أو مزود Riverpod يجب أن يحافظ على سلوك CRUD والتصدير والإشعارات.

## بوابات القبول

بعد كل مرحلة يجب أن ينجح `scripts/static_check.sh`. وبسبب عدم توفر Flutter محلياً، يجب أن ينجح GitHub Actions في التحليل والاختبارات وبناء APK قبل اعتبار العمل مكتملاً.

## تاريخ الإنشاء

2026-08-20
