# فحص نظام الإشعارات وإضافة إشعارات الغياب

يوثق هذا التقرير نظام الإشعارات في تطبيق **سجل الطالب** كما كان قبل التعديل، ثم يوضح الميزة المضافة: **إشعارات الغياب عند بلوغ حد الفصل** مع قسم جديد في الإعدادات يحدد مقدار هذا الحد.

---

## أولاً: الفحص الحالي لنظام الإشعارات

### 1. مكوّنات النظام

| المكوّن | الملف | المسؤولية |
| --- | --- | --- |
| خدمة الإشعارات | `lib/core/notifications/notification_service.dart` | تغليف `flutter_local_notifications`، إنشاء القنوات، طلب الصلاحيات، عرض الإشعار وإلغاؤه، وتوجيه ضغط المستخدم. |
| وحدة التحكم | `lib/core/providers.dart` | حساب الحدود بعد كل عملية حفظ، واتخاذ القرار بإرسال إشعار النظام. |
| زر الإشعارات | `lib/features/dashboard/presentation/dashboard_page.dart` | الجرس في رأس الصفحة الرئيسية مع شارة العدد ولوحة الإشعارات. |
| بوابة التشغيل | `lib/main.dart` | تهيئة الخدمة، وفتح ملف الطالب عند الضغط على إشعار النظام. |
| الإعدادات | `lib/features/settings/presentation/settings_page.dart` | طلب صلاحيات الإشعارات، وضبط حدود الغياب والسلوك. |

### 2. ماذا كان يظهر داخل زر الإشعارات قبل التعديل

زر الإشعارات في الصفحة الرئيسية كان يعرض **الإشعارات السلوكية فقط**:

- شارة العدد (`Badge.count`) تساوي عدد الطلاب الذين بلغوا حد التنبيه السلوكي أو حد الفصل بالنقاط.
- عند الضغط تُفتح لوحة سفلية بعنوان «الإشعارات السلوكية» تعرض لكل طالب:
  - الاسم الكامل،
  - الحرف الأول من الاسم داخل دائرة (أحمر عند حد الفصل، وثانوي عند التنبيه)،
  - سطراً فرعياً: `حد الفصل • الدرجة السلوكية 55` أو `تنبيه • الدرجة السلوكية 42`،
  - سهم يفتح ملف الطالب عند الضغط على السطر.
- عند عدم وجود حالات تظهر حالة فراغ: «لا توجد تنبيهات سلوكية».

### 3. فكرة عمل الإشعار في المشروع

تعتمد الفكرة على **حساب الحدود لحظياً من البيانات المحلية** بدلاً من تخزين حالة إشعار منفصلة:

1. يسجّل المعلم واقعة (سلوك جديد مثلاً) من الواجهة.
2. تُستدعى دالة في `AppController` تُجري العملية على قاعدة Isar ثم تُعيد تحميل اللقطة (`AppSnapshot`).
3. يُحسب الملخص **قبل** التعديل ويُحفظ مؤقتاً، ثم يُحسب **بعد** التعديل.
4. إذا انتقل الطالب من حالة مستقرة إلى حالة تنبيه أو فصل، تُستدعى خدمة الإشعارات لعرض إشعار نظام فوري، بينما تُحدَّث شارة زر الإشعارات تلقائياً لأن الواجهة تُبنى من اللقطة نفسها.
5. إشعار النظام يحمل الحمولة `student:<uuid>`؛ وعند ضغطه يفتح `main.dart` ملف الطالب مباشرة حتى لو كان التطبيق مغلقاً.

القنوات ومعرّفات الإشعارات:

| القناة | المعرّف | الأهمية | الاستخدام |
| --- | --- | --- | --- |
| المتابعة والتنبيهات | `almoktaber_follow_up` | `high` | الإشعارات المجدولة للمتابعة. |
| الإشعارات السلوكية | `student_behavior_alerts` | `max` | التنبيهات السلوكية الفورية. |
| إشعارات الغياب (جديد) | `student_absence_alerts` | `max` | تنبيهات بلوغ حد الغياب. |

معرّف إشعار السلوك = `100000 + (hash(uuid) % 100000000)`، ومعرّف إشعار الغياب = `200000 + (hash(uuid) % 100000000)`، أي أن إشعارَي الطالب نفسه لا يستبدل أحدهما الآخر ويمكن إلغاؤهما بشكل مستقل. أما Windows فيستخدم هوية `AlMoktaber.StudentRecord` مع إشعار Toast فوري بنفس الحمولة.

---

## ثانياً: الميزة المضافة — إشعارات الغياب

### 1. القاعدة المتبعة في الاحتساب

- يُحتسب **الغياب بدون عذر** فقط (`AttendanceStatus.absent`).
- لا تدخل الإجازة (`leave`) ولا الغياب بعذر (`excused`) ولا التأخر (`late`) في الحد.
- **حد التنبيه** (`absenceWarningThreshold`): عند بلوغه يظهر الطالب في زر الإشعارات بدرجة «تنبيه».
- **حد الفصل** (`absenceDismissalThreshold`): عند بلوغه يظهر الطالب بدرجة «حد الفصل» ويُرسل إشعار نظام فوري.

القيم الافتراضية: حد التنبيه **5** أيام، وحد الفصل **10** أيام، ويمكن تغييرها من الإعدادات.

### 2. قسم الإعدادات الجديد

أُضيف قسم **«إشعارات الغياب»** في صفحة الإعدادات بعد قسم «نقاط السلوك»، ويحتوي:

- حقل **حد تنبيه الغياب** (بالأيام)،
- حقل **حد فصل الغياب** (بالأيام)،
- شرح للقاعدة المتبعة في الاحتساب،
- زر **حفظ إعدادات الغياب**.

التحقق عند الحفظ:

- القيم أعداد صحيحة موجبة (يوم واحد على الأقل)،
- `حد التنبيه ≤ حد الفصل`، وإلا تظهر رسالة واضحة دون حفظ.

### 3. ما تغيّر داخل زر الإشعارات

| قبل | بعد |
| --- | --- |
| الإشعارات السلوكية فقط | إشعارات السلوك **والغياب** معاً |
| شارة العدد = عدد التنبيهات السلوكية | شارة العدد = مجموع تنبيهات السلوك والغياب |
| لوحة بعنوان «الإشعارات السلوكية» | لوحة بعنوان «الإشعارات» مع مرشحات: الكل / الغياب / السلوك وعدد كل نوع |
| السطر: الاسم + الدرجة السلوكية | السطر: الاسم + درجة الإشعار + السبب + تاريخ آخر واقعة، مع شارة نوع الإشعار (غياب/سلوك) |

لوحة الإشعارات انتقلت إلى ملف مستقل `lib/features/notifications/presentation/notifications_sheet.dart` بدل أن تكون داخل صفحة الرئيسية، وأصبحت قابلة لإعادة الاستخدام، مع بقاء الانتقال إلى ملف الطالب عند الضغط على أي سطر.

### 4. تحسين في آلية التنبيه

أصبح قرار إرسال إشعار النظام يعتمد على **مقارنة رتبة الإشعار** لا على وجود إشعار سابق فقط:

| ترتيب الحد | الرتبة |
| --- | --- |
| لا إشعار | 0 |
| تنبيه | 1 |
| حد الفصل | 2 |

- لا يُرسل إشعار مكرر لنفس الرتبة (تجنّب الإزعاج).
- يُرسل إشعار جديد عند **الانتقال** من التنبيه إلى حد الفصل، لأن الحالة أصبحت أخطر.
- ينطبق المنطق نفسه على السلوك والغياب عبر دوال مشتركة داخل `AppController`.

### 5. الملفات المضافة والمعدّلة

| الملف | الحالة | الوصف |
| --- | --- | --- |
| `lib/core/attendance/attendance_summary.dart` | جديد | حساب ملخص المواظبة (غياب بدون عذر، بعذر، تأخر، إجازة، حضور) وحدود التنبيه والفصل. |
| `lib/core/notifications/student_alert.dart` | جديد | نموذج الإشعار الموحّد وبناء قائمة الإشعارات مرتبة حسب الخطورة ثم الاسم. |
| `lib/features/notifications/presentation/notifications_sheet.dart` | جديد | لوحة الإشعارات مع المرشحات وحالة الفراغ. |
| `lib/core/database/isar_models.dart` | معدّل | حقلان جديدان في `AppSettings` + كائن `AbsenceThresholds` للقيم الافتراضية والضبط. |
| `lib/core/database/isar_models.g.dart` | معدّل | تحديث الملف المولّد ليطابق ناتج `build_runner` مع الحقلين الجديدين. |
| `lib/core/database/local_repository.dart` | معدّل | حفظ الحدود، التحقق منها، ضبط البيانات القديمة، ورفع إصدار النسخة الاحتياطية إلى 3. |
| `lib/core/database/local_store.dart` | معدّل | توسيع واجهة التخزين بحدود الغياب. |
| `lib/core/notifications/notification_service.dart` | معدّل | قناة الغياب، معرّف مستقل لكل إشعار، ودالة عرض موحّدة. |
| `lib/core/providers.dart` | معدّل | ضبط الحدود، وإرسال إشعار الغياب بعد تسجيل الحضور. |
| `lib/features/dashboard/presentation/dashboard_page.dart` | معدّل | الجرس وشارته يعتمدون على الإشعارات الموحدة. |
| `lib/features/settings/presentation/settings_page.dart` | معدّل | قسم «إشعارات الغياب» ومنطق حفظه. |
| `lib/features/students/presentation/student_details_page.dart` | معدّل | بطاقة «تنبيه غياب / إشعار فصل بالغياب» في ملف الطالب. |
| `lib/features/students/presentation/students_page.dart` | معدّل | مرشح «تنبيهات الغياب» يعتمد على الحدود المضبوطة في الإعدادات. |
| `test/attendance_summary_test.dart` | جديد | اختبارات الاحتساب والحدود والقيم الافتراضية. |
| `test/student_alerts_test.dart` | جديد | اختبارات قائمة الإشعارات الموحدة وترتيبها. |

### 6. التوافق مع البيانات الحالية

- الحقلان الجديدان يُخزَّنان كأعداد صحيحة في مجموعة `AppSettings`، ويدعم Isar إضافة الخصائص إلى مخطط قائم دون تهجير يدوي للبيانات.
- الصفوف القديمة التي لا تحتوي القيمتين تُضبط تلقائياً على القيم الافتراضية عند أول تحميل، وتُحفظ مرة واحدة.
- النسخ الاحتياطية: أُضيف قسم `attendance` داخل إعدادات النسخة، ورُفع `schemaVersion` إلى 3 مع قبول الإصدارات 1 و2 عند الاستعادة (تُستخدم فيها القيم الافتراضية).

---

## ثالثاً: التحقق والاختبار

### فحوص آلية

```bash
./scripts/static_check.sh
flutter analyze
flutter test
```

يعمل خط CI في `.github/workflows/build-flutter.yml` بهذا الترتيب بعد توليد ملفات Isar عبر `dart run build_runner build --delete-conflicting-outputs`، ثم بناء نسخة APK موقّعة ونسخة Windows.

### قائمة تحقق يدوية

1. سجّل غياباً بدون عذر حتى بلوغ حد التنبيه، وتأكد من ظهور الطالب في زر الإشعارات بدرجة «تنبيه» دون إشعار نظام مزعج متكرر.
2. أكمل الغياب إلى حد الفصل، وتأكد من ظهور إشعار نظام فوري بعنوان «إشعار غياب»، ومن ظهور الطالب في أعلى القائمة بدرجة «حد الفصل».
3. اضغط على الإشعار من خارج التطبيق، وتأكد من فتح ملف الطالب الصحيح.
4. غيّر حد التنبيه وحد الفصل من الإعدادات، وتأكد من انتقال الطالب بين الدرجات أو خروجه من القائمة بعد التعديل.
5. جرّب استعادة نسخة احتياطية قديمة (إصدار 1 أو 2) وتأكد من ظهور القيم الافتراضية لحدود الغياب.
6. تحقق من عمل المرشحات (الكل / الغياب / السلوك) ومن ظهور السلوك والغياب لنفس الطالب في سطرين مستقلين.

---

## المراجع

1. [Isar Schema — الأنواع المدعومة والتغييرات المتوافقة](https://isar-community.dev/v3/schema.html): القيم الافتراضية للأعداد، وإمكانية تغيير الحقول دون تهجير يدوي.
2. [Isar Discussion #345 — Support schema migration](https://github.com/isar/isar/discussions/345): «Isar will handle all the index/link/property additions/deletions internally» مع صيغة تخزين تدعم التغييرات المتوافقة رجعياً.
3. [flutter_local_notifications — القنوات والصلاحيات](https://pub.dev/packages/flutter_local_notifications): إنشاء قنوات Android، وطلب الأذونات، وصيغة `show` و`zonedSchedule`.
4. [دعم Windows 10 وWindows 11 والإشعارات](WINDOWS_NOTIFICATIONS.md): تفاصيل منصة Windows وهوية MSIX المستخدمة مع الإشعارات.
