# فحص نظام الإشعارات وميزة إشعارات الغياب

يوثّق هذا الملف نتيجة فحص نظام الإشعارات في **سجل الطالب** قبل إضافة الميزة الجديدة، ثم يشرح ميزة **إشعارات حد الفصل للغياب** التي أُضيفت بناءً على هذا الفحص.

## أولاً: نتيجة فحص نظام الإشعارات الحالي

### 1. ما الذي كان يظهر داخل زر الإشعارات قبل التعديل؟

زر الإشعارات هو زر الجرس أعلى الصفحة الرئيسية (مع شارة عدد `Badge.count`). كان يفتح لوحة سفلية تعرض **الإشعارات السلوكية فقط**:

| نوع الإشعار | شرط الظهور | المصدر |
| --- | --- | --- |
| تنبيه سلوكي | الدرجة السلوكية للطالب ≥ `warningThreshold` (حد التنبيه) | سجلات السلوك المخزنة |
| إشعار حد الفصل | الدرجة السلوكية للطالب ≥ `dismissalThreshold` (حد الفصل) | سجلات السلوك المخزنة |

لم يكن هناك أي إشعار مرتبط بالغياب، رغم أن التطبيق يحتفظ بسجل حضور كامل (`AttendanceRecord` بحالة `absent`).

### 2. فكرة وطريقة عمل الإشعارات في المشروع

يعتمد النظام على مسارين متوازيين يعملان من نفس البيانات:

**أ. الإشعارات داخل التطبيق (زر الجرس في الرئيسية):**

1. يعيد `LocalRepository.loadSnapshot()` تحميل «لقطة» كاملة من قاعدة Isar المحلية: الإعدادات، الطلاب، الحضور، السلوك، إلخ (`AppSnapshot`).
2. عند بناء الصفحة الرئيسية تُحسب التنبيهات من اللقطة مباشرة (بدون تخزين إضافي): لكل طالب يُجمع مجموع نقاط السلوك عبر `calculateBehaviorSummary` ثم يُقارن بحدّي التنبيه/الفصل في `AppSettings`.
3. الطلاب الذين تجاوزوا الحدود يظهرون في لوحة الإشعارات، والضغط على أي اسم يفتح ملف الطالب.

هذا المسار «محسوب لحظياً»؛ لا يخزن قائمة إشعارات، بل يشتقها من البيانات في كل مرة. لذلك أي تعديل في الحضور أو السلوك يظهر في الزر تلقائياً بعد التحديث.

**ب. الإشعارات النظامية (خارج التطبيق - Android/Windows):**

1. `NotificationService` تلفّ حزمة `flutter_local_notifications` وتُهيّئها في `main()` مع قناتين: `almoktabar_follow_up` (المتابعة) و`student_behavior_alerts` (السلوك)، وإعدادات Windows Toast مستقلة (AppUserModelID ثابت + GUID).
2. عند **إضافة أو تعديل سجل سلوك** عبر `AppController` تُلتقط حالة الطالب قبل الحفظ وبعده؛ إذا عبر الطالب أحد الحدود السلوكية **لأول مرة** (لم يكن متجاوزاً قبل التعديل) يُرسل إشعار نظامي فوري بعنوان «إشعار سلوكي: اسم الطالب».
3. الضغط على الإشعار النظامي يعيد المستخدم إلى التطبيق عبر `payload` بصيغة `student:<uuid>` يفتح ملف الطالب (`onNotificationTap` في `main.dart`).
4. تُطلب صلاحية الإشعارات عند أول إرسال، ويمكن إعادة طلبها من: الإعدادات ← الإشعارات والخصوصية ← صلاحيات التنبيهات.

معرّفات إشعارات السلوك مشتقة من hash الخاص بـ `uuid` الطالب في النطاق `100000..100099999` حتى يستبدل الإشعار الجديد القديم لنفس الطالب بدل التراكم.

## ثانياً: الميزة الجديدة — إشعارات الغياب

### السلوك المطلوب والمُنفّذ

- عند بلوغ الطالب **عدد أيام الغياب** (حالة «غائب» فقط) المحدد في الإعدادات أو تجاوزه:
  - يُضاف الطالب إلى **زر الإشعارات** في الرئيسية كبطاقة مستقلة توضح «بلغ حد الفصل للغياب» وعدد الأيام.
  - يُرسل **إشعار نظامي فوري** مرة واحدة عند لحظة العبور (وليس عند البقاء فوق الحد) عبر قناة جديدة.
- حذف سجل غياب أو تعديله إلى «حاضر/بعذر/متأخر/إجازة» يُخرج الطالب من الإشعارات تلقائياً لأن الحساب لحظي.

### قسم الإعدادات الجديد

قسم **«حد الفصل للغياب»** في صفحة الإعدادات (بين «نقاط السلوك» و«إدارة البيانات»):

- حقل رقمي بعدد الأيام (لا يقبل صفراً أو كسوراً) مع زر حفظ مستقل.
- القيمة تُخزن في `AppSettings.absenceDismissalThreshold` (القيمة الافتراضية 3 أيام) وتُضمّن في النسخ الاحتياطية JSON تحت المفتاح `absence.dismissalThreshold` مع توافق كامل مع النسخ القديمة.

### التفاصيل التقنية

| العنصر | القرار |
| --- | --- |
| محرك التنبيهات الموحد | ملف جديد `lib/core/alerts/student_alerts.dart` يبني `List<StudentAlert>` نقية من اللقطة، وتجمع كل طالب في بطاقة واحدة حتى لو تجاوز السلوك والغياب معاً، مرتبة بحسب الخطورة (حد الفصل ← الغياب ← التنبيه السلوكي) |
| عدّ الغياب | عدد سجلات الحضور بحالة `AttendanceStatus.absent` لكل طالب (نفس تعريف «الغياب» المعروض في ملف الطالب والتقارير)؛ الغياب بعذر لا يُحتسب |
| قناة الإشعار النظامي | `student_absence_alerts` باسم «إشعارات الغياب» (Android) مع نفس تفاصيل Windows Toast |
| معرّف الإشعار | `absenceNotificationId` في نطاق `200000..200099999` مستقل عن نطاق السلوك حتى لا يلغي كلٌّ منهما الآخر |
| نقطة التشغيل | `setAttendance` / `updateAttendance` / `deleteAttendance` في `AppController` تلتقط حالة «فوق الحد» قبل الحفظ، وتُرسل الإشعار بعد الحفظ عند العبور الجديد فقط (نفس نمط إشعارات السلوك) |
| هجرة قاعدة البيانات | حقل جديد في `AppSettings`؛ Isar يهجر المخطط تلقائياً بالاسم ويملأ الحقل بالقيمة الافتراضية دون فقدان بيانات |
| صياغة الأيام | دالة `absenceDaysLabel` بصياغة عربية سليمة (يوم واحد، يومان، 3-10 أيام، 11+ يوماً) |

### صيغة نص الإشعار النظامي

- العنوان: `إشعار غياب: <اسم الطالب>`
- النص: `بلغ <عدد الأيام بصياغة عربية> وهو ما يساوي حد الفصل المحدد (<الحد> أيام) أو يجاوزه.`

## ثالثاً: الاختبارات

- `test/student_alerts_test.dart` (جديد): يغطي الشرط الحدي (لا تنبيه قبل الحد، تنبيه عند بلوزه)، استثناء الغياب بعذر، دمج تنبيهي السلوك والغياب لطالب واحد مع ترتيب الخطورة، تعطيل الميزة عند الحد = 0، وصياغة عدد الأيام.
- `test/behavior_summary_test.dart` يبقى كما هو دون تغيير.

## رابعاً: خطوات التحقق اليدوي

1. الإعدادات ← حد الفصل للغياب ← اجعل الحد 3 واحفظ.
2. من شاشة الحضور سجّل 3 أيام غياب لنفس الطالب: يظهر إشعار نظامي عند الغياب الثالث فقط، ويظهر الطالب داخل زر الإشعارات في الرئيسية.
3. عدّل أحد الأيام إلى «بعذر»: يختفي الطالب من زر الإشعارات فوراً.
4. صدّر نسخة JSON وتأكد من وجود `absence.dismissalThreshold`، ثم استعدها وتأكد من بقاء الحد.
