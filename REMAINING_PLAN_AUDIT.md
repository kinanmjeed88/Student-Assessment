# تدقيق خطة التحسين المتبقية

## النتيجة العامة

النسخة الحالية مستقرة وقابلة للبناء، لكنها ما زالت تحتوي على فجوات في فصل المسؤوليات وتوحيد تجربة النماذج والتصفية والاستجابة. توجد عمليات CRUD حقيقية في `AppController` و`LocalRepository` لمعظم الكيانات، إلا أن بعض الشاشات لا تعرض تجربة موحدة أو لا تستفيد من كل الحقول المتاحة.

## الفجوات المؤكدة من قراءة الكود

1. `AppController` ما زال يجمع مسؤوليات التنسيق بين Riverpod والمستودع والإشعارات والتحقق، ما يحتاج إلى فصل واجهة المستودع وخدمة حالات الاستخدام دون تغيير مصدر البيانات.
2. شاشات الدرجات والحضور والسلوك والملاحظات تحتوي على عمليات حقيقية، لكن النماذج والتصفية وتجربة التعديل والحذف ليست موحدة بالكامل عبر الميزات.
3. شاشة الطلاب تحتاج إلى مراجعة شاملة للتصفية المتسلسلة حسب الصف ثم الشعبة، وتوحيد العرض المختصر للاسم والصف والشعبة، وتقليل التكرار البصري في الحاويات.
4. شاشة الصفوف تحتاج إلى مراجعة عرض الصف والشعبة وإدارة الإضافة والتعديل والحذف مع الحفاظ على ربط الطلاب بالشعبة عند التصفية.
5. شاشة ملف الطالب كبيرة ومركبة وتجمع العرض والنماذج والتصدير، وتحتاج إلى تقليل مسؤوليات العرض المباشرة وتحسين الاستجابة وتوحيد بطاقات السجلات.
6. إعدادات نقاط السلوك وبيانات المؤسسة والحركة والإشعارات موجودة في النموذج والواجهة، وتحتاج إلى تحقق تكاملي من تأثيرها المباشر عند إنشاء سجل سلوك جديد وعرض السطر المؤسسي.
7. خدمة الإشعارات موجودة ومربوطة بـ AppController، وتحتاج إلى مراجعة دورة الحياة والصلاحيات والرسائل حتى يكون تنبيه السلوك قابلاً للاستخدام على Android دون تكرار غير مقصود.
8. قواعد الفحص تمنع `textAlign` والألوان الثابتة و`FilePicker.platform`، ويجب تشغيل `static_check.sh` و`git diff --check` بعد كل مجموعة تغييرات.

## معيار الإكمال

يُعد العمل مكتملاً عند تحقق جميع ما يلي: توحيد النماذج والمكونات المشتركة، وجود تصفية صف ثم شعبة في الشاشات ذات الصلة، CRUD مكتمل لكل سجل قابل للإضافة، تطبيق إعدادات السلوك فعلياً، ربط بيانات المؤسسة بالحركة، تشغيل إشعارات Android، مرور الفحص الساكن والتحليل والاختبارات، نجاح بناء APK الموقّع في GitHub Actions، ونظافة شجرة Git بعد الرفع.
