# تقرير الهجرة الهندسية إلى Flutter

## الملخص التنفيذي

تم اعتماد الرؤية المعمارية بنسبة **100% على مستوى القرارات والنطاق** بعد تثبيت Riverpod، وIsar، والعمل المحلي Offline-First، وسياسة JSON للاستعادة، وتكافؤ الوظائف، وهوية Android، وقواعد RTL، وبنية الإشعارات. أُنشئ داخل المستودع مجلد مستقل باسم `flutter_app` يحافظ على مشروع React Native/Expo القديم كما هو ويقدم أساس Flutter أصلياً قابلاً للمراجعة اليدوية.

هذا التسليم هو **Baseline هندسي قابل للفحص** وليس ادعاءً بإصدار APK نهائي؛ فبيئة الاستقصاء لا تحتوي على Flutter أو Dart SDK، ولذلك لم يتم تشغيل `flutter analyze` أو `flutter test` أو بناء APK/AAB داخل الجلسة. كما أن ملف `isar_models.g.dart` ملف مولد عمداً ولا يُكتب يدوياً، ويُنشأ بعد تشغيل `build_runner` في بيئة Flutter CI. هذه الحدود موثقة صراحة لتجنب اعتماد إصدار لم يمر ببوابة الترجمة الفعلية.

## نتيجة استقصاء المشروع القديم

المشروع القديم هو تطبيق Expo/React Native محلي لإدارة الطلاب. نقطة الجذر `app/_layout.tsx` تدير شجرة التنقل وموفر الحالة، بينما يحتوي مسار التبويبات على لوحات الطلاب والفصول والحضور والتقارير والإعدادات. نموذج الحالة في `lib/student-store.tsx` هو مصدر الحقيقة الفعلي للكيانات والعمليات، ويستخدم التخزين المحلي؛ أما مخطط Drizzle في `drizzle/schema.ts` فلا يمثل قاعدة المجال المحلية التي يعتمد عليها الهاتف.

| نطاق Expo القديم | القرار في Flutter | موضع التنفيذ الجديد |
|---|---|---|
| `lib/student-store.tsx` | استبدال الحالة والتخزين المركزيين بـ Isar وRiverpod | `core/database/isar_models.dart`, `core/database/local_repository.dart`, `core/providers.dart` |
| `app/_layout.tsx` | استبدال الجذر الملاحي بجذر Material 3 عربي | `lib/main.dart`, `features/dashboard/presentation/app_shell.dart` |
| شاشة الطلاب | قائمة RTL محسنة مع بحث و`ListView.builder` وإضافة وحذف متسلسل | `features/students/presentation/students_page.dart` |
| شاشة تفاصيل الطالب | صفحة ملف مستقلة قابلة لتوسعة الحضور والدرجات والسلوك والملاحظات | `features/students/presentation/student_details_page.dart` |
| شاشة الفصول والشعب | إنشاء وعرض وحذف الفصل مع تحذير الحذف المتسلسل | `features/classes/presentation/classes_page.dart` |
| شاشة الحضور | تسجيل حالة يومية لكل طالب وتاريخ | `features/presence/presentation/attendance_page.dart` و`LocalStore.setAttendance` |
| `app/import-students.tsx` | إبقاء الاستيراد كميزة مستقلة ودعم XLS/XLSX/CSV عبر حد خدمة Excel | `features/imports/`، مع عقد التنفيذ في المرحلة التالية |
| `lib/backup.ts` | تصدير JSON شامل بصيغة UTF-8 واستعادة لاحقة بعقد إصدار مخطط | `features/settings/data/backup_service.dart` و`LocalStore.exportBackupJson` و`restoreBackupJson` |
| شاشة الإعدادات | بيانات المدرسة، صلاحيات التنبيهات، والنسخة الاحتياطية | `features/settings/presentation/settings_page.dart` |

## الهيكل المعماري المقترح

الهيكل Feature-First، بينما تجمع `core` القدرات المشتركة التي لا تنتمي إلى ميزة واحدة. تعتمد الواجهات على عقود مجردة، وتتعامل Riverpod مع حقن التبعيات ودورة الحالة، في حين يظل Isar محصوراً في طبقة البيانات. هذا يمنع تسرب تفاصيل قاعدة البيانات إلى Widgets ويجعل استبدال التنفيذ في الاختبارات ممكناً.

```text
flutter_app/
├── android/
│   └── app/
│       ├── build.gradle
│       └── src/main/AndroidManifest.xml
├── lib/
│   ├── main.dart
│   ├── core/
│   │   ├── database/
│   │   │   ├── database_service.dart
│   │   │   ├── isar_models.dart
│   │   │   ├── isar_models.g.dart        # generated by build_runner
│   │   │   ├── local_store.dart
│   │   │   └── local_repository.dart
│   │   ├── notifications/
│   │   │   └── notification_service.dart
│   │   ├── theme/
│   │   │   └── app_theme.dart
│   │   └── providers.dart
│   └── features/
│       ├── dashboard/presentation/
│       │   ├── app_shell.dart
│       │   └── dashboard_page.dart
│       ├── students/
│       │   ├── data/
│       │   ├── domain/
│       │   └── presentation/
│       │       ├── students_page.dart
│       │       └── student_details_page.dart
│       ├── classes/
│       │   ├── data/
│       │   ├── domain/
│       │   └── presentation/classes_page.dart
│       ├── presence/presentation/attendance_page.dart
│       ├── imports/{data,domain,presentation}/
│       ├── reports/{data,domain,presentation}/
│       └── settings/
│           ├── data/backup_service.dart
│           └── presentation/settings_page.dart
├── test/
├── analysis_options.yaml
├── pubspec.yaml
└── README.md
```

## مخطط Isar وقواعد المجال

تعتمد Isar على Collections مولدة من annotations، مع `@Index` للمعرفات الخارجية والبحث بالاسم، و`@enumerated` للقيم المحددة. يطابق هذا أسلوب التهيئة الموثق في Isar، حيث تُعرّف Collection وتولّد schema ثم تُفتح قاعدة البيانات بتمرير الـ schemas إلى `Isar.open` [1].

| Collection | المفاتيح والفهارس | سبب وجودها |
|---|---|---|
| `AppSettings` | `id = 1` | إعدادات المدرسة والمعلم والعام الدراسي وقواعد الخصم |
| `SchoolClass` | `uuid` فريد | الصف/الفصل الأساسي |
| `Section` | `uuid` فريد، `classUuid` مفهرس | شعب الصف وعلاقتها بالصف |
| `Student` | `uuid` فريد، `fullName`، `classUuid`، `sectionUuid` | الهوية والبيانات الأساسية وولي الأمر |
| `AttendanceRecord` | `studentUuid + date` فريد مركب | حالة حضور واحدة لكل طالب في اليوم |
| `GradeField` | `uuid` فريد | حقل مستقل لكل درجة كما طلبت قاعدة المجال |
| `Grade` | `studentUuid + fieldUuid` فريد مركب | درجة الطالب داخل الحقل المستقل |
| `BehaviorRecord` | `uuid` فريد، `studentUuid` مفهرس | السلوك والمخالفة والإجراء والمتابعة |
| `StudentNote` | `uuid` فريد، `studentUuid` مفهرس | الملاحظات الأكاديمية والصحية والتربوية |
| `StudentImportRecord` | `uuid` فريد | سجل الاستيراد وقابلية التراجع عنه |

تنفذ `deleteClassCascade` و`deleteStudentCascade` داخل `writeTxn` واحدة. قبل حذف الصف، تجمع العملية الشعب والطلاب ثم تحذف الحضور والدرجات والسلوك والملاحظات وسجلات الاستيراد والطلاب والشعب ثم الصف. وتبقى قيمة `penaltyPoints` محفوظة داخل `BehaviorRecord` لحظة الإنشاء، بدلاً من إعادة حسابها من إعدادات لاحقة؛ وبذلك تُحفظ قاعدة تثبيت الخصم حرفياً.

تصدير JSON يتضمن `schemaVersion` ووقت التصدير وجميع Collections، وتنفذ `restoreBackupJson` فحص الإصدار ثم استبدال البيانات داخل `writeTxn` واحدة مع محولات صريحة للتعدادات والتواريخ ومفاتيح JSON القديمة. لم يُنفّذ ترحيل مباشر من AsyncStorage، التزاماً بقرارك؛ نقطة التوافق الرسمية هي ملف JSON الناتج من النسخة القديمة، مع إبقاء مسار Adapter منفصلاً إذا اختلفت أسماء مفاتيح النسخة القديمة.

## الجذر والـ RTL ونظام التصميم

`main.dart` يهيئ الإطار قبل `runApp`، ويهيئ خدمة الإشعارات، ثم يحقنها عبر Riverpod. يضبط `MaterialApp` اللغة العربية و`flutter_localizations`، ويضع `Directionality(textDirection: TextDirection.rtl)` في `builder` الجذري. لا يعتمد الأساس على `textAlign` لمحاكاة الاتجاه، ولا على إزاحات سالبة أو عكس Padding، ولا يحتوي على `GestureDetector`؛ التفاعل البصري يمر عبر `InkWell` داخل Material أو عبر `ElevatedButton`.

يحتوي `AppTheme.light()` على `ColorScheme` من Material 3، ومسافات إدخال، وأحجام نص لا تقل عن 14 للنص المساعد و16 للنص الأساسي، وبطاقات وأزرار موحدة. كل شاشة رئيسية تقع تحت `SafeArea`، والقوائم الطويلة تستخدم `ListView.builder`. كما أن الحذف يطلب تأكيداً صريحاً، والنجاح يظهر برسالة خضراء في الإعدادات والحفظ.

## الإشعارات Android

تم تثبيت `flutter_local_notifications: 17.2.4` كما طلبت، مع فصل التهيئة في `NotificationService`. التهيئة تنشئ channel باسم `almoktaber_follow_up`، وتطلب صلاحية `POST_NOTIFICATIONS` على Android 13+، وتطلب صلاحية exact alarms عندما تستدعيها Workflow الفعلي، وتوفر `scheduleFollowUp` باستخدام `zonedSchedule` و`AndroidScheduleMode.exactAllowWhileIdle`. توضح الوثائق الرسمية للحزمة إعداد Android والقنوات والصلاحيات والجدولة [2].

يحتوي Manifest على `POST_NOTIFICATIONS` و`SCHEDULE_EXACT_ALARM` و`RECEIVE_BOOT_COMPLETED`، وعلى receivers الخاصة بالجدولة وإعادة الإقلاع. لم تُربط بعد قواعد العمل الخاصة بالحضور والمتابعة السلوكية بمعرفات إشعارات نهائية؛ هذا ربط domain workflow وليس قراراً بنيوياً، ويمكن إضافته دون تعديل الجذر أو Widgets.

## الحزم المثبتة

| الحزمة | الإصدار/الدور |
|---|---|
| `flutter_riverpod` | `^3.4.2` لإدارة الحالة بحقن Notifier/AsyncNotifier |
| `isar`, `isar_flutter_libs` | `^3.1.0+1` للتخزين المحلي وسلامة المعاملات |
| `isar_generator`, `build_runner` | توليد مخططات Isar |
| `flutter_local_notifications` | `17.2.4` للإشعارات المحلية المجدولة |
| `timezone` | تحويل مواعيد الجدولة إلى المنطقة الزمنية المحلية |
| `file_picker` | اختيار وحفظ ملفات النسخ والاستيراد |
| `excel_plus` | قراءة XLS وXLSX وتدفقات CSV وفق وصف الحزمة [5] |
| `pdf`, `printing` | إنشاء ومشاركة/طباعة PDF؛ توثق الحزمتان `pw.Document` و`Printing.sharePdf` [6] [7] |
| `path_provider`, `uuid`, `intl` | مسارات النظام، معرفات مستقرة، وتنسيق التواريخ |

يُستخدم Riverpod الحديث عبر `AsyncNotifier` بدلاً من الأنماط القديمة التي تضع منطقاً موزعاً داخل `StateNotifier` وProvider؛ هذا يتوافق مع توجيه Riverpod الرسمي حول مركزية منطق التهيئة والتعديلات داخل Notifier [3].

## بوابات البناء والاعتماد

أضيف الملف `.github/workflows/build-flutter.yml` إلى المستودع. يعمل المسار عند تغييرات `flutter_app/**` أو تشغيل يدوي، ويثبت Java 17 وFlutter Stable، ويولد Android platform files إذا لم تكن موجودة، ثم ينفذ `pub get` و`build_runner` و`static_check.sh` و`flutter analyze` و`flutter test` ويبني APK وAAB ويرفعهما كـ artifacts. إذا لم تتوفر أسرار التوقيع، يستمر المسار في بناء مخرجات Release غير موقعة لغرض التحقق؛ أما التوقيع فيُفعل فقط عند توفير الأسرار الأربعة `FLUTTER_KEYSTORE_BASE64` و`FLUTTER_KEYSTORE_PASSWORD` و`FLUTTER_KEY_ALIAS` و`FLUTTER_KEY_PASSWORD`.


قبل اعتماد الإصدار يجب تنفيذ الأوامر التالية في بيئة Flutter CI، بالترتيب:

```bash
cd flutter_app
flutter pub get
dart run build_runner build --delete-conflicting-outputs
flutter analyze
flutter test
flutter build apk --release
flutter build appbundle --release
```

يجب توفير `key.properties` وملف keystore خارج Git، مع بقاء `applicationId` و`namespace` على `com.almoktaber`. إعداد Gradle المضاف يقرأ مفاتيح التوقيع من الملف الخارجي، ولا يحتوي على أسرار. بعد البناء يجب اختبار استعادة JSON، والحذف المتسلسل، والقوائم الطويلة، وحالة لوحة المفاتيح مع النوافذ السفلية، وصلاحية التنبيهات على Android 13 و14 و15.

الفحص الساكن المرفق في `scripts/static_check.sh` نجح داخل البيئة الحالية، وتحقق من وجود Directionality وIsar ونسخة الإشعارات و`ListView.builder` وعدم وجود `GestureDetector` أو محاكاة RTL يدوية. لكنه لا يستبدل `flutter analyze`؛ غياب Flutter/Dart SDK منع تنفيذ فحص الترجمة فعلياً.

## ما تم تسليمه وما لم يُدّعَ إنجازه

تم تسليم `main.dart`، ونظام التصميم، وجذر التنقل، ونماذج Isar، وخدمة فتح القاعدة، وعقد المستودع، وتنفيذ CRUD والحذف المتسلسل والحضور والاستعادة والتصدير JSON، وخدمة الإشعارات، وشاشات Dashboard والطلاب والتفاصيل والفصول والحضور والإعدادات، وإعدادات Manifest وGradle الأساسية. نجح `scripts/static_check.sh` بعد هذه الإضافات.

أما القراءة التفصيلية لمحتوى XLS/XLSX/CSV وتصدير تقارير PDF/Excel كاملة مع اسم المدرسة والمعلم والعام الدراسي وتلوين التحذيرات بالأحمر، وشاشة الاستعادة والتحقق من `schemaVersion`، وشاشات الدرجات والسلوك والملاحظات، فهي وحدات Feature مستقلة لم تُختزل في Widgets وهمية داخل هذا التسليم. هذا الفصل مقصود للحفاظ على Zero Patching: تُبنى كل وحدة على عقد Isar وUse Case واختبارات قبول منفصلة بدلاً من حشوها في `main.dart` أو المستودع العام.

## المراجع

[1]: https://pub.dev/packages/isar "Isar — official pub.dev documentation"
[2]: https://pub.dev/packages/flutter_local_notifications/versions/17.2.4 "flutter_local_notifications 17.2.4 — official pub.dev documentation"
[3]: https://riverpod.dev/docs/migration/from_state_notifier "Riverpod — official Notifier migration guidance"
[4]: https://pub.dev/packages/isar_generator "Isar generator — official pub.dev documentation"
[5]: https://pub.dev/packages/excel_plus "excel_plus — XLS/XLSX/CSV package documentation"
[6]: https://pub.dev/packages/pdf "pdf — Dart PDF creation documentation"
[7]: https://pub.dev/packages/printing "printing — Flutter PDF share and print documentation"
