# خطة العمل: إصلاح التصدير وإضافة تنزيل أسماء الطلاب

## أولاً: المشكلة في «التقارير والبيانات»

### التشخيص
كان توليد ملفات Excel في `lib/core/services/report_service.dart` يعتمد على `Sheet.appendRow()`
مع تنسيق الخلايا بمؤشر صف محسوب يدوياً (`row++`). هذه الطريقة هشّة لأن:

1. `appendRow` يكتب في الصف التالي المحسوب داخلياً من `maxRows`، بينما كان التنسيق يُكتب في
   مؤشر صف يُدار يدوياً. عند اختلاف المؤشرين تُنشأ خلايا فارغة وتُستبدل بيانات الصفوف،
   فيظهر صف واحد فقط في الملف الناتج.
2. دمج الخلايا (`merge`) لعنوان التقرير يغيّر `maxRows` داخلياً فيزيد الانحراف.
3. ورقة العمل الافتراضية `Sheet1` تبقى داخل المصنّف وتُفتح أولاً في Excel،
   فيبدو أن الملف يحتوي على «ورقة واحدة» فارغة.

### الحل
- كتابة جميع الخلايا بشكل صريح عبر `updateCell(CellIndex.indexByColumnRow(...))` من خلال
  مُساعد داخلي `_SheetWriter` يملك مؤشر صف واحد فقط كمصدر وحيد للحقيقة
  (single source of truth)، فلا يمكن أن يختلف موضع القيمة عن موضع التنسيق.
- كل صف من صفوف البيانات يُكتب بالكامل بلا استثناء، مع عدّاد `dataRowCount` يُستخدم في
  سطر الإجمالي.
- حذف ورقة `Sheet1` الافتراضية وضبط الورقة الافتراضية على أول ورقة حقيقية،
  حتى تظهر جميع أوراق التقرير عند الفتح.
- تذييل الإجماليات لا يستخدم الدمج بعد الآن (كان يكتب في العمود 2 حتى لو كانت الورقة
  بعمودين فقط) بل يكتب التسمية والقيمة في عمودين صالحين دائماً.

## ثانياً: زر تنزيل أسماء الطلاب للصف/الشعبة المحددة

- `ReportService.exportClassStudentsXlsx(snapshot, classUuid, sectionUuid)` ينتج ملف Excel
  يحتوي الطلاب المطابقين للمرشح الحالي فقط، مرتّبين بنفس ترتيب العرض في الواجهة
  (ت، الاسم الكامل، رقم الطالب، الصف، الشعبة، الجنس، الحالة، ولي الأمر، الهاتف).
- زر في شريط صفحة الطلاب يظهر ويُفعَّل فقط عند اختيار صف (وشعبة اختيارياً)،
  ويُسمّي الملف تلقائياً باسم الصف والشعبة.

## ثانياً (ب): تصحيح اتجاه الأعمدة داخل الملف

### التشخيص
كان `ExcelReportBuilder.addSheet` يفعل أمرين في الوقت نفسه:

1. `sheet.isRTL = true`، وهو ما تكتبه الحزمة كـ `<sheetView rightToLeft="1"/>`،
   فيعرض Excel العمود A في أقصى اليمين والعمود B إلى يساره وهكذا.
2. عكس الأعمدة فيزيائياً (`columns.reversed` و`values.reversed`) قبل الكتابة.

النتيجة انعكاس مزدوج: العمود A يحمل آخر عمود منطقي («هاتف ولي الأمر») ويُعرض
في أقصى اليمين، فيقرأ المستخدم الجدول كأنه إنكليزي من اليسار إلى اليمين ويظهر
عمود «ت» ثم «الاسم الكامل» في أقصى اليسار.

### الحل
- إبقاء `sheet.isRTL = true` وحده مسؤولاً عن الاتجاه، وكتابة الترويسة والقيم
  وعروض الأعمدة بالترتيب المنطقي دون عكس فيزيائي. أصبح العمود A هو «ت»
  فيظهر في أقصى اليمين ويليه «الاسم الكامل» ثم «رقم الطالب» وهكذا.
- مطابقة البيانات المخزّنة للترتيب المنطقي تجعل الملف قابلاً للقراءة من أي
  أداة أخرى، وهذا ما يعتمد عليه الاستيراد الدائري أدناه.
- إلغاء إعادة الفرز في `exportClassStudentsXlsx` حتى يبقى ترتيب الصفوف مطابقاً
  لترتيب العرض في صفحة الطلاب (الفرز الإضافي غير مستقر وقد يبدّل مواقع
  الأسماء المتطابقة).

### الأثر على الاستيراد
القارئ القديم في `ImportExportService._readWorkbook` كان يلتقط أول خلية غير
فارغة في كل صف، فيقرأ رقم الهاتف (قبل الإصلاح) أو رقم التسلسل (بعده) بدل
الاسم عند استيراد ملف أسماء صدّره التطبيق نفسه. لذلك أُضيف
`lib/core/services/workbook_names_reader.dart` الذي يتعرف على عمود الاسم من
الترويسة (مع استثناء «رقم الطالب» و«هاتف ولي الأمر»)، ويتجاوز أسطر العنوان
والوصف والإجمالي، ويرفض القيم الرقمية، ويعمل على نصوص مجردة بلا اعتماد على
حزمة Excel حتى يصلح للفروع الثلاثة.

## ثالثاً: التدقيق

- اختبارات وحدة في `test/report_service_test.dart` تفك ترميز المصنّف الناتج وتتحقق من:
  عدد الصفوف = عدد السجلات، وجود جميع الأوراق، غياب `Sheet1`، وتصفية الصف/الشعبة.
- اختبارات اتجاه الأعمدة: الترويسة بالترتيب المنطقي (أولها «ت» ثم «الاسم
  الكامل» وآخرها «هاتف ولي الأمر») في ملف أسماء الصف، وأول عمود في تقارير
  الطلاب والحضور، مع التحقق من تطابق ترتيب الصفوف مع ترتيب العرض ومن أن
  الملف المصدَّر يعيد الأسماء نفسها عند استيراده.
- اختبارات `test/workbook_names_reader_test.dart` لقارئ الأسماء: التعرف على
  عمود الاسم، تجاوز التسلسل الرقمي، القوائم البسيطة بلا ترويسة، الترويسات
  الإنكليزية، وإزالة التكرار بين الأوراق.
- `scripts/verify_export_rtl.py` يفحص ثوابت الاتجاه في الباني والخدمة دون
  الحاجة إلى أدوات Flutter.
- `flutter analyze` و `flutter test` عبر سير عمل GitHub Actions.

## رابعاً: النشر

تُطبَّق التعديلات على الفرع الرئيسي `main` (فرع أندرويد) وعلى فرعي ويندوز ٧
وويندوز ١٠/١١. يتم النقل إلى فرعي ويندوز بالتقاط commit الإصلاح وحده عبر
`scripts/sync_windows_branches.sh`، لا بدمج `main` فيهما، لأن الدمج ينقل
مجلد `android` وواجهات ممنوعة في فرع وندوز ٧ ويعيد مزايا حُذفت عمداً من فرع
وندوز ١٠/١١. التفاصيل في `WINDOWS_BRANCH_SYNC.md`.
